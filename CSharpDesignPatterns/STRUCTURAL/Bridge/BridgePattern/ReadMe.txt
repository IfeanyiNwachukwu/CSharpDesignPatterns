Bridge
The intent of this pattern is to decouple an abstraction from its
implementation so the two can vary independently


Use Cases for the Bridge Pattern
When you want to avoid a permanent binding between an
abstraction and its implementation (to enable switching
implementations at runtime)
When abstraction and implementations should be extensible by
subclassing
When you don’t want changes in the implementation of an
abstraction have an imp